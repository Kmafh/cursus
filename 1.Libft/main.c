#include "libft.h"
#include <stdio.h>
#include <stdio.h>
int main(void)
{
       
      char * chad="algo";

     char * ch;
     ch=ft_memset(chad,';',4);
      printf("1 = true 0=false : %s\n",ch);
      return(0);
}